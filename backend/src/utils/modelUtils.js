exports.formatModelInput = (block) => {
  // This is a placeholder. Implement the logic to format the input
  // for different model types based on your requirements.
  return JSON.stringify(block);
};

exports.parseModelOutput = (output, blockType) => {
  // This is a placeholder. Implement the logic to parse the output
  // from different models based on your requirements.
  return JSON.parse(output);
};